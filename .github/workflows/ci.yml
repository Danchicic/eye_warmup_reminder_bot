name: ci.yml
on:
  push:
    branches: [ "main" ]
jobs:
  setup_docker:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Docker
        run: docker --version

      - name: Build Docker Image
        run: docker build -t my-bot .
